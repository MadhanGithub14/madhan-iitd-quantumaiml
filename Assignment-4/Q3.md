# Question 3 — CNOT from H and CZ only
# Build an exact copy of CNOT using only H and CZ gates, no cx allowed, and verify the matrix matches

Construction and math:
  - Identity: CNOT_{control,target} = (I ⊗ H) · CZ_{control,target} · (I ⊗ H)
  - Apply `H` to the target before and after a `CZ` acting between control and target.

Proof sketch (matrix-level):
- `CZ` = diag(1,1,1,-1) in the computational basis.
- Conjugating `CZ` by H on the target transforms the conditional phase into a conditional bit-flip (i.e., CNOT).


<img width="661" height="517" alt="image-6" src="https://github.com/user-attachments/assets/91ada1df-deb0-4761-a92f-8edf65356e60" />



Composer build:
1. Two qubits.
2. Apply `H` on target qubit.
3. Apply `CZ` between control and target.
4. Apply `H` on target again.
5. Verify by comparing unitary (if Composer provides), or run state preparations on basis inputs and verify mapping:
   - |00⟩ -> |00⟩
   - |01⟩ -> |01⟩
   - |10⟩ -> |11⟩
   - |11⟩ -> |10⟩

<img width="1915" height="937" alt="image-11" src="https://github.com/user-attachments/assets/697204f4-c19c-4dbc-b954-a29385cfd85e" />
